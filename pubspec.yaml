name: adventures_in_video_chat
description: Video Chat for Adventuring in Dart, Flutter, Firebase.

publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_webrtc: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
